#include <stdio.h>
#include <stdlib.h>

#include "include/macro.h"
#include "usual.c"


/*
int main() {
    getchar();
    char str[10];
    fgets(str, sizeof(str), stdin);
    if(*str == '\n'){

    }
    printf("[%s]", str);


    return 0;
}*/

int main() {

    flush_input_buffer();
}